# URL base do backend Django (sem barra final). Os pedidos usam `${EXPO_PUBLIC_API_URL}/api/...`.
EXPO_PUBLIC_API_URL=http://127.0.0.1:8000

# Domínio institucional sem `@` (alinhado a INSTITUTIONAL_EMAIL_DOMAIN no backend).
EXPO_PUBLIC_INSTITUTIONAL_EMAIL_DOMAIN=aluno.wyden.edu.br
